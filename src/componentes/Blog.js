import React from 'react';

const Blog = () => {
    return ( 
        <div>
            <h1>Blog</h1>
            <p>You can pass a function ("interpolations") to a styled component's template literal to adapt it based on its props. This button component has a primary state that changes its color. When setting the primary prop to true, we are swapping out its background and text color. </p>
        </div>
     );
}
 
export default Blog